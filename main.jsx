import React from "react";
import ReactDOM from "react-dom/client";
import MeerwerkCalculator from "./MeerwerkCalculator";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MeerwerkCalculator />
  </React.StrictMode>
);
